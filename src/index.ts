// TODO: Implement asm-scope-parser
// Planned exports: parseScope(input: string | string[]) → NormalizedScope
// NormalizedScope methods: .includes(target), .excludes(target), .toCIDR(), .toJSON()

export {};
